# API Base Configuration
api.timeout=30000
api.retry.count=3
api.retry.delay=1000
api.follow.redirects = False
api.url.encoding.enabled = False

# Reporting Configuration
report.name=API Test Report
report.screenshots=true
report.logs=true

# Logging Configuration
logging.level=INFO